# Settings for Backend (on Cloud Run).
# See https://firebase.google.com/docs/app-hosting/configure#cloud-run
runConfig:
  minInstances: 0
  # maxInstances: 100
  # concurrency: 80
  # cpu: 1
  # memoryMiB: 512

# Environment variables and secrets.
# Note: Fragrantica scraping disabled in production (Cloudflare blocks cloud browsers)
# Run scripts/backfillMetadata.ts locally to populate notes for added fragrances
# env:
#   - variable: EXAMPLE
#     value: example

